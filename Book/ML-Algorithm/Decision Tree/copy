
关于python里的copy。
如果a = np.array(???)
此时，如果b = a
那么不管a、b哪个改变，另一个都会改变。
变量、列表也是一样。

但是如果
a = np.ones(5)
b = np.zeros(5)
b[0] = a[0]
a[0] = 100
此时b[0]还是1,不会随着a改变而改变，因为这时a和b是两个不同的引用，所以赋值只是单>纯的赋值而不是引用传递。


